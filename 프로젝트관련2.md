## Zoo Management System Engine - 공통 인터페이스 설계

### 1. 메뉴 관리 인터페이스
```java
/**
/**
 * 제네릭 타입을 이용해 각 엔티티의 저장소에서 마지막으로 사용된 ID를 조회하고
 * 숫자 부분을 +1 하여 접미사(A/E/K)를 붙여 새로운 ID를 생성하는 패턴을 문서화합니다.
 *
 * 설계 의도:
 * - 생성 로직은 저장소(Repository)에서 조회한 마지막 ID를 바탕으로 계산합니다.
 * - IdGenerator는 제네릭 메서드를 제공하여 어떤 엔티티 타입에서도 동일한 방식으로 ID를 생성합니다.
 * - 저장소는 `getAllIds()` 또는 `getLastId()` 같은 메서드를 제공해야 합니다.
 */
public final class IdGenerator {

    private IdGenerator() { /* 유틸리티 클래스: 인스턴스화 금지 */ }

    /**
     * 저장소에서 마지막 ID를 조회해 다음 ID를 생성합니다.
     * @param repository ID를 관리하는 저장소(엔티티별 구현)
     * @param suffix 생성할 ID의 접미자(예: 'A','E','K')
     * @param <T> 엔티티 타입
     * @return 생성된 ID (예: "003A")
     *
     * 사용 예시:
     * <pre>
     * // 저장소는 엔티티별로 구현되어 있어야 함
     * Repository<Animal> animalRepo = new AnimalRepositoryImpl();
     * String next = IdGenerator.generateNextId(animalRepo, 'A'); // "001A" 또는 "002A" 등
     * </pre>
     */
    public static <T> String generateNextId(Repository<T> repository, char suffix) {
        // 저장소는 마지막 ID를 Optional로 반환하도록 권장
        java.util.Optional<String> lastOpt = repository.getLastId();
        int nextNumber = lastOpt
                .map(IdGenerator::getSequenceNumberSafe)
                .map(n -> n + 1)
                .orElse(1);

        return String.format("%03d%c", nextNumber, suffix);
    }

    // 타입별 편의 메서드
    public static <T> String generateNextAnimalId(Repository<T> repository) { return generateNextId(repository, 'A'); }
    public static <T> String generateNextEnclosureId(Repository<T> repository) { return generateNextId(repository, 'E'); }
    public static <T> String generateNextKeeperId(Repository<T> repository) { return generateNextId(repository, 'K'); }

    // 내부 유틸: ID의 숫자 파트 안전 추출 (예: "005A" -> 5)
    private static int getSequenceNumberSafe(String id) {
        if (id == null || id.length() < 2) return 0;
        String digits = id.substring(0, id.length() - 1);
        try {
            return Integer.parseInt(digits);
        } catch (NumberFormatException e) {
            return 0;
        }
    }

    /**
     * ID 포맷 검증 유틸 (숫자3자리 + 접미사) - 필요하면 저장소 레벨에서 사용
     */
    public static boolean isValidIdFormat(String id, char expectedSuffix) {
        if (id == null) return false;
        if (id.length() < 2) return false;
        char suffix = id.charAt(id.length() - 1);
        String numberPart = id.substring(0, id.length() - 1);
        try {
            Integer.parseInt(numberPart);
            return suffix == expectedSuffix;
        } catch (NumberFormatException ex) {
            return false;
        }
    }
}

// ----------------------------
// Repository(저장소) 계약 예시
// ----------------------------
/**
 * 저장소는 엔티티별로 구현되며, ID 조회를 위해 최소한 다음 메서드를 제공해야 합니다.
 * 실제 구현은 DB, 파일, 메모리 등 어떤 저장소든 가능.
 */
interface Repository<T> {
    /**
     * 저장소에 존재하는 마지막(가장 큰) ID를 Optional로 반환한다.
     * 저장소가 빈 경우 Optional.empty()를 반환.
     */
    java.util.Optional<String> getLastId();

    // ...existing repository methods (save, find, getAll 등)
}

/* 사용 예시:
 *
 * class AnimalRepositoryImpl implements Repository<Animal> {
 *     // 내부적으로 DB나 Map을 사용. 예: List<String> ids = loadAllIds();
 *     public Optional<String> getLastId() {
 *         // ids가 비어있으면 Optional.empty()
 *         // 아니면 가장 큰 수치의 ID를 반환 (정렬하거나 스트림 max)
 *     }
 * }
 *
 * // 사용 위치: 엔티티 등록 시
 * String newAnimalId = IdGenerator.generateNextId(animalRepository, 'A');
 * animal.setId(newAnimalId);
 * animalRepository.save(animal);
 */
     * </pre>
     */
    T getByName(String name);
    
    /**
     * ID로 엔티티 정보를 수정합니다.
     * @param id 수정할 엔티티의 자동 생성된 ID
     * @param updatedEntity 수정할 정보 (ID는 변경되지 않음)
     * @return 수정 성공 여부
     * 
     * 사용 예시:
     * <pre>
     * Animal updatedTiger = new Animal("시베리아 호랑이", "시베리아호랑이");
     * boolean success = animalManager.updateById("001A", updatedTiger);
     * if (success) {
     *     System.out.println("동물 정보가 수정되었습니다. ID는 변경되지 않습니다.");
     * }
     * </pre>
     */
    boolean updateById(ID id, T updatedEntity);
    
    /**
     * 이름으로 엔티티 정보를 수정합니다.
     * @param name 수정할 엔티티의 이름
     * @param updatedEntity 수정할 정보 (ID는 변경되지 않음)
     * @return 수정 성공 여부
     * 
     * 사용 예시:
     * <pre>
     * Animal updatedTiger = new Animal("백호", "백호종");
     * boolean success = animalManager.updateByName("호랑이", updatedTiger);
     * // 기존 ID(001A)는 유지되고 이름과 종류만 변경됨
     * </pre>
     */
    boolean updateByName(String name, T updatedEntity);
    
    /**
     * ID로 엔티티를 삭제합니다.
     * @param id 삭제할 엔티티의 자동 생성된 ID
     * @return 삭제 성공 여부
     * 
     * 사용 예시:
     * <pre>
     * boolean deleted = animalManager.removeById("001A");
     * if (deleted) {
     *     System.out.println("ID 001A의 동물이 삭제되었습니다.");
     * } else {
     *     System.out.println("삭제할 동물을 찾을 수 없습니다.");
     * }
     * </pre>
     */
    boolean removeById(ID id);
    
    /**
     * 이름으로 엔티티를 삭제합니다.
     * @param name 삭제할 엔티티의 이름
     * @return 삭제 성공 여부
     * 
     * 사용 예시:
     * <pre>
     * boolean deleted = animalManager.removeByName("호랑이");
     * // 이름으로 찾아서 해당 동물과 자동 생성된 ID를 함께 삭제
     * </pre>
     */
    boolean removeByName(String name);
    
    /**
     * 다음 사용 가능한 ID를 생성합니다.
     * 엔티티 타입에 따라 접미사가 결정됩니다 (A: Animal, E: Enclosure, K: Keeper).
     * @return 자동 생성된 고유 ID
     * 
     * 사용 예시:
     * <pre>
     * String newAnimalId = animalManager.generateNextId();
     * // 반환값: "003A" (기존에 001A, 002A가 있다면)
     * 
     * String newEnclosureId = enclosureManager.generateNextId();
     * // 반환값: "001E" (첫 번째 케이지라면)
     * 
     * String newKeeperId = keeperManager.generateNextId();
     * // 반환값: "001K" (첫 번째 사육사라면)
     * </pre>
     */
    ID generateNextId();
}
```

### 3. 입력 처리 인터페이스
```java
/**
 * 사용자 입력 처리를 담당하는 인터페이스
 * 안전한 입력 처리와 검증 기능을 제공합니다.
 * 
 * 사용 예시:
 * <pre>
 * InputHandler inputHandler = new ConsoleInputHandler();
 * int userChoice = inputHandler.getIntInput(1, 5); // 1-5 범위 내 입력
 * String animalName = inputHandler.getStringInput(20); // 최대 20자
 * boolean confirmed = inputHandler.getConfirmation("삭제하시겠습니까?");
 * </pre>
 */
public interface InputHandler {
    
    /**
     * 안전한 정수 입력을 받습니다.
     * 잘못된 입력 시 재입력을 요구합니다.
     * @return 유효한 정수 값
     * 
     * 사용 예시:
     * <pre>
     * System.out.print("메뉴를 선택하세요: ");
     * int choice = inputHandler.getIntInput();
     * // 사용자가 "abc" 입력 시 "올바른 숫자를 입력해주세요" 메시지 후 재입력 요구
     * </pre>
     */
    int getIntInput();
    
    /**
     * 안전한 정수 입력을 받습니다 (범위 제한).
     * @param min 최소값
     * @param max 최대값
     * @return 범위 내의 유효한 정수 값
     * 
     * 사용 예시:
     * <pre>
     * int menuChoice = inputHandler.getIntInput(0, 4);
     * // 0-4 범위 밖의 숫자 입력 시 재입력 요구
     * System.out.println("선택한 메뉴: " + menuChoice);
     * </pre>
     */
    int getIntInput(int min, int max);
    
    /**
     * 안전한 문자열 입력을 받습니다.
     * 공백 문자를 제거하여 반환합니다.
     * @return 유효한 문자열
     * 
     * 사용 예시:
     * <pre>
     * System.out.print("동물 이름을 입력하세요: ");
     * String animalName = inputHandler.getStringInput();
     * // "  호랑이  " 입력 시 "호랑이" 반환
     * </pre>
     */
    String getStringInput();
    
    /**
     * 안전한 문자열 입력을 받습니다 (길이 제한).
     * @param maxLength 최대 길이
     * @return 길이 제한 내의 유효한 문자열
     * 
     * 사용 예시:
     * <pre>
     * String cageName = inputHandler.getStringInput(10);
     * // 10자 초과 입력 시 재입력 요구
     * </pre>
     */
    String getStringInput(int maxLength);
    
    /**
     * 확인/취소 입력을 받습니다.
     * @param message 확인 메시지
     * @return true(확인), false(취소)
     * 
     * 사용 예시:
     * <pre>
     * boolean confirmed = inputHandler.getConfirmation("정말로 삭제하시겠습니까?");
     * if (confirmed) {
     *     // 삭제 실행
     *     deleteAnimal();
     * } else {
     *     System.out.println("삭제가 취소되었습니다.");
     * }
     * </pre>
     */
    boolean getConfirmation(String message);
}
```

### 4. 결제 처리 인터페이스
```java
/**
 * 관람객의 결제 및 금액 관리를 담당하는 인터페이스
 * 입장료, 체험비 등의 결제 기능을 제공합니다.
 * 
 * 사용 예시:
 * <pre>
 * PaymentProcessor paymentProcessor = new ZooPaymentProcessor();
 * int currentMoney = 10000;
 * int remainingMoney = paymentProcessor.processPayment(currentMoney, 5000, "입장료");
 * paymentProcessor.showCurrentMoney(remainingMoney);
 * </pre>
 */
public interface PaymentProcessor {
    
    /**
     * 결제를 처리합니다.
     * @param currentMoney 현재 소지금액
     * @param price 결제할 금액
    ## MVP: 간단한 ID 자동 생성 예제 (간소화)

    간단한 동작 증명을 위한 최소 기능(MVP) 코드를 제공합니다. 핵심 아이디어는 "저장소에서 마지막 ID를 조회하여 숫자만 +1 한 뒤 접미사(A/E/K)를 붙여 새 ID를 만드는 것"입니다.

    ```java
    // --- 엔티티 (간단 버전)
    class Animal {
            private String id;
            private final String name;
            public Animal(String name) { this.name = name; }
            public String getId() { return id; }
            public void setId(String id) { this.id = id; }
            public String getName() { return name; }
    }

    // --- 저장소 계약 (간단)
    interface Repository<T> {
            java.util.Optional<String> getLastId();
            void save(T entity);
    }

    // --- Animal 전용 메모리 저장소 (MVP)
    class AnimalRepository implements Repository<Animal> {
            private final java.util.List<Animal> list = new java.util.ArrayList<>();
            public java.util.Optional<String> getLastId() {
                    if (list.isEmpty()) return java.util.Optional.empty();
                    return java.util.Optional.ofNullable(list.get(list.size()-1).getId());
            }
            public void save(Animal a) { list.add(a); }
    }

    // --- 간소화된 IdGenerator
    final class IdGenerator {
            private IdGenerator() {}
            public static <T> String generateNextId(Repository<T> repo, char suffix) {
                    int next = repo.getLastId().map(id -> Integer.parseInt(id.substring(0, id.length()-1)) + 1).orElse(1);
                    return String.format("%03d%c", next, suffix);
            }
    }

    // --- 간단한 사용 예시
    class MainMVP {
            public static void main(String[] args) {
                    AnimalRepository repo = new AnimalRepository();
                    Animal a1 = new Animal("Tiger");
                    a1.setId(IdGenerator.generateNextId(repo, 'A'));
                    repo.save(a1);

                    Animal a2 = new Animal("Elephant");
                    a2.setId(IdGenerator.generateNextId(repo, 'A'));
                    repo.save(a2);

                    System.out.println(a1.getId() + " " + a1.getName()); // 001A Tiger
                    System.out.println(a2.getId() + " " + a2.getName()); // 002A Elephant
            }
    }
    ```

    간결한 설계 포인트:
    - 저장소는 "마지막 ID"만 제공하면 됨.
    - IdGenerator는 저장소를 주입받아 숫자를 올리고 접미사를 붙임.
    - 실제 환경에서는 동시성(경합) 처리가 필요하므로 DB의 트랜잭션 또는 분산 ID 생성 전략을 고려해야 함.

    ### Mermaid 클래스 다이어그램 (MVP 기준)

    ```mermaid
    classDiagram
            class Animal {
                -String id
                -String name
                +getId()
                +setId()
            }

            class Repository~T~ {
                +getLastId()
                +save(T)
            }

            class AnimalRepository {
                -List~Animal~ list
                +getLastId()
                +save(Animal)
            }

            class IdGenerator {
                +generateNextId(Repository, char)
            }

            class MainMVP {
                +main()
            }

            Repository <|-- AnimalRepository
            AnimalRepository o-- Animal : stores
            MainMVP --> AnimalRepository : uses
            MainMVP --> IdGenerator : uses
    ```
    /**
     * 현재 메뉴에서 이전 메뉴로 돌아갑니다.
     * 
     * 사용 예시:
     * <pre>
     * navigator.navigateBack();
     * // 현재가 동물관리 메뉴라면 관리자 메뉴로 돌아감
     * </pre>
     */
    void navigateBack();
}
```

### 6. 출력 처리 인터페이스
```java
/**
 * 시스템의 다양한 출력 기능을 담당하는 인터페이스
 * 일관된 형식의 메시지와 정보 표시를 제공합니다.
 * 
 * 사용 예시:
 * <pre>
 * OutputHandler outputHandler = new ConsoleOutputHandler();
 * outputHandler.showWelcomeMessage("동물원 관리 시스템");
 * outputHandler.showSuccessMessage("동물이 등록되었습니다");
 * List<String> animals = Arrays.asList("호랑이", "코끼리", "기린");
 * outputHandler.showFormattedList(animals, "등록된 동물 목록");
 * </pre>
 */
public interface OutputHandler {
    
    /**
     * 성공 메시지를 표시합니다.
     * @param message 성공 메시지
     * 
     * 사용 예시:
     * <pre>
     * outputHandler.showSuccessMessage("동물이 성공적으로 등록되었습니다");
     * // 출력: "✅ 동물이 성공적으로 등록되었습니다"
     * </pre>
     */
    void showSuccessMessage(String message);
    
    /**
     * 오류 메시지를 표시합니다.
     * @param message 오류 메시지
     * 
     * 사용 예시:
     * <pre>
     * outputHandler.showErrorMessage("해당 ID의 동물을 찾을 수 없습니다");
     * // 출력: "❌ 해당 ID의 동물을 찾을 수 없습니다"
     * </pre>
     */
    void showErrorMessage(String message);
    
    /**
     * 정보 메시지를 표시합니다.
     * @param message 정보 메시지
     * 
     * 사용 예시:
     * <pre>
     * outputHandler.showInfoMessage("현재 등록된 동물은 총 5마리입니다");
     * // 출력: "ℹ️ 현재 등록된 동물은 총 5마리입니다"
     * </pre>
     */
    void showInfoMessage(String message);
    
    /**
     * 환영 메시지를 표시합니다.
     * @param systemName 시스템 이름
     * 
     * 사용 예시:
     * <pre>
     * outputHandler.showWelcomeMessage("동물원 관리 시스템");
     * // 출력: "=== 동물원 관리 시스템에 오신 것을 환영합니다 ==="
     * </pre>
     */
    void showWelcomeMessage(String systemName);
    
    /**
     * 구분선을 표시합니다.
     * 
     * 사용 예시:
     * <pre>
     * outputHandler.showSeparator();
     * // 출력: "================================================"
     * </pre>
     */
    void showSeparator();
    
    /**
     * 리스트를 포맷팅하여 표시합니다.
     * @param items 표시할 항목들
     * @param title 리스트 제목
     * 
     * 사용 예시:
     * <pre>
     * List<String> cages = Arrays.asList("A-1: 맹수관", "B-2: 초식동물관");
     * outputHandler.showFormattedList(cages, "케이지 목록");
     * // 출력: "=== 케이지 목록 ==="
     * //      "1. A-1: 맹수관"
     * //      "2. B-2: 초식동물관"
     * </pre>
     */
    void showFormattedList(List<?> items, String title);
}
## Zoo Management System Engine - ID 자동 생성 유틸리티

### 7. ID 생성 유틸리티 인터페이스
```java
/**
 * 동물원 시스템의 고유 ID 자동 생성을 담당하는 유틸리티 인터페이스
 * 각 엔티티 타입별로 고유한 접미사를 가진 ID를 생성합니다.
 * 
 * ID 형식:
 * - 동물(Animal): 001A, 002A, 003A...
 * - 케이지(Enclosure): 001E, 002E, 003E...
 * - 사육사(Keeper): 001K, 002K, 003K...
 * 
 * 사용 예시:
 * <pre>
 * String animalId = IdGenerator.generateAnimalId();      // "001A"
 * String enclosureId = IdGenerator.generateEnclosureId(); // "001E"
 * String keeperId = IdGenerator.generateKeeperId();       // "001K"
 * </pre>
 */
public interface IdGenerator {
    
    /**
     * 동물용 고유 ID를 생성합니다.
     * @return 동물 ID (형식: 001A, 002A, ...)
     * 
     * 사용 예시:
     * <pre>
     * String newAnimalId = IdGenerator.generateAnimalId();
     * Animal tiger = new Animal(newAnimalId, "호랑이", "아시아호랑이");
     * System.out.println("생성된 동물 ID: " + newAnimalId); // "001A"
     * </pre>
     */
    static String generateAnimalId() {
        return generateId("A");
    }
    
    /**
     * 케이지용 고유 ID를 생성합니다.
     * @return 케이지 ID (형식: 001E, 002E, ...)
     * 
     * 사용 예시:
     * <pre>
     * String newEnclosureId = IdGenerator.generateEnclosureId();
     * Enclosure cage = new Enclosure(newEnclosureId, "맹수관", 100.0);
     * System.out.println("생성된 케이지 ID: " + newEnclosureId); // "001E"
     * </pre>
     */
    static String generateEnclosureId() {
        return generateId("E");
    }
    
    /**
     * 사육사용 고유 ID를 생성합니다.
     * @return 사육사 ID (형식: 001K, 002K, ...)
     * 
     * 사용 예시:
     * <pre>
     * String newKeeperId = IdGenerator.generateKeeperId();
     * Keeper keeper = new Keeper(newKeeperId, "김동물", 5);
     * System.out.println("생성된 사육사 ID: " + newKeeperId); // "001K"
     * </pre>
     */
    static String generateKeeperId() {
        return generateId("K");
    }
    
    /**
     * 지정된 접미사로 고유 ID를 생성하는 내부 메서드입니다.
     * @param suffix ID 접미사 (A: Animal, E: Enclosure, K: Keeper)
     * @return 생성된 고유 ID
     * 
     * 내부 구현 예시:
     * <pre>
     * private static String generateId(String suffix) {
     *     // 현재 최대 번호를 데이터베이스에서 조회
     *     int maxNumber = getMaxNumberBySuffix(suffix);
     *     int nextNumber = maxNumber + 1;
     *     return String.format("%03d%s", nextNumber, suffix);
     * }
     * </pre>
     */
    static String generateId(String suffix) {
        // 구현체에서 실제 로직을 제공해야 함
        throw new UnsupportedOperationException("구현체에서 제공해야 합니다.");
    }
    
    /**
     * ID의 유효성을 검증합니다.
     * @param id 검증할 ID
     * @param expectedSuffix 기대되는 접미사
     * @return 유효성 여부
     * 
     * 사용 예시:
     * <pre>
     * boolean isValidAnimal = IdGenerator.isValidId("001A", "A");    // true
     * boolean isValidAnimal2 = IdGenerator.isValidId("001E", "A");   // false
     * boolean isValidFormat = IdGenerator.isValidId("ABC", "A");     // false
     * </pre>
     */
    static boolean isValidId(String id, String expectedSuffix) {
        if (id == null || id.length() != 4) {
            return false;
        }
        
        String numberPart = id.substring(0, 3);
        String suffixPart = id.substring(3);
        
        try {
            Integer.parseInt(numberPart);
            return suffixPart.equals(expectedSuffix);
        } catch (NumberFormatException e) {
            return false;
        }
    }
    
    /**
     * ID에서 순번을 추출합니다.
     * @param id 대상 ID
     * @return 순번 (숫자 부분)
     * 
     * 사용 예시:
     * <pre>
     * int sequenceNumber = IdGenerator.getSequenceNumber("005A"); // 5
     * int sequenceNumber2 = IdGenerator.getSequenceNumber("010E"); // 10
     * </pre>
     */
    static int getSequenceNumber(String id) {
        if (id == null || id.length() != 4) {
            throw new IllegalArgumentException("유효하지 않은 ID 형식입니다: " + id);
        }
        
        try {
            return Integer.parseInt(id.substring(0, 3));
        } catch (NumberFormatException e) {
            throw new IllegalArgumentException("ID의 숫자 부분이 유효하지 않습니다: " + id);
        }
    }
}
import java.util.Scanner;

public class Main {
    private static Scanner scanner = new Scanner(System.in);

    public static void main(String[] args) {
        System.out.println("=== 동물원 관리 시스템에 오신 것을 환영합니다 ===");
        showMainMenu();
        scanner.close();
    }

    // 메인 메뉴 표시 및 처리
    private static void showMainMenu() {
        while (true) {
            System.out.println("\n당신은 누구입니까?");
            System.out.println("1. 관리자  2. 관람객  0. 종료");
            System.out.print("번호를 입력해주세요: ");

            int choice = getIntInput();

            switch (choice) {
                case 1 -> handleAdminMenu();
                case 2 -> handleVisitorMenu();
                case 0 -> {
                    System.out.println("시스템을 종료합니다. 감사합니다.");
                    return;
                }
                default -> System.out.println("잘못된 입력입니다. 다시 선택해주세요.");
            }
        }
    }

    // 관리자 메뉴 처리
    private static void handleAdminMenu() {
        while (true) {
            System.out.println("\n=== 관리자 메뉴 ===");
            System.out.println("1. 동물관리  2. 케이지관리  3. 사육사관리  0. 메인메뉴로");
            System.out.print("선택해주세요: ");

            int choice = getIntInput();

            switch (choice) {
                case 1 -> handleAnimalManagement();
                case 2 -> handleCageManagement();
                case 3 -> handleKeeperManagement();
                case 0 -> {
                    System.out.println("메인 메뉴로 돌아갑니다.");
                    return;
                }
                default -> System.out.println("잘못된 입력입니다. 다시 선택해주세요.");
            }
        }
    }

    // 동물 관리 메뉴
    private static void handleAnimalManagement() {
        while (true) {
            System.out.println("\n=== 동물 관리 ===");
            System.out.println("1. 동물등록  2. 동물조회  3. 동물수정  4. 동물삭제  0. 뒤로가기");
            System.out.print("선택해주세요: ");

            int choice = getIntInput();

            switch (choice) {
                case 1 -> registerAnimal();
                case 2 -> viewAnimals();
                case 3 -> updateAnimal();
                case 4 -> deleteAnimal();
                case 0 -> {
                    System.out.println("이전 메뉴로 돌아갑니다.");
                    return;
                }
                default -> System.out.println("잘못된 입력입니다. 다시 선택해주세요.");
            }
        }
    }

    // 케이지 관리 메뉴
    private static void handleCageManagement() {
        while (true) {
            System.out.println("\n=== 케이지 관리 ===");
            System.out.println("1. 케이지등록  2. 케이지조회  3. 케이지수정  4. 케이지삭제  0. 뒤로가기");
            System.out.print("선택해주세요: ");

            int choice = getIntInput();

            switch (choice) {
                case 1 -> registerCage();
                case 2 -> viewCages();
                case 3 -> updateCage();
                case 4 -> deleteCage();
                case 0 -> {
                    System.out.println("이전 메뉴로 돌아갑니다.");
                    return;
                }
                default -> System.out.println("잘못된 입력입니다. 다시 선택해주세요.");
            }
        }
    }

    // 사육사 관리 메뉴
    private static void handleKeeperManagement() {
        while (true) {
            System.out.println("\n=== 사육사 관리 ===");
            System.out.println("1. 사육사등록  2. 사육사조회  3. 사육사수정  4. 사육사삭제  0. 뒤로가기");
            System.out.print("선택해주세요: ");

            int choice = getIntInput();

            switch (choice) {
                case 1 -> registerKeeper();
                case 2 -> viewKeepers();
                case 3 -> updateKeeper();
                case 4 -> deleteKeeper();
                case 0 -> {
                    System.out.println("이전 메뉴로 돌아갑니다.");
                    return;
                }
                default -> System.out.println("잘못된 입력입니다. 다시 선택해주세요.");
            }
        }
    }

    // 관람객 메뉴 처리 - 소지금액 관리 개선
    private static void handleVisitorMenu() {
        System.out.println("\n=== 관람객 메뉴 ===");
        System.out.print("당신의 이름을 입력해주세요: ");
        String visitorName = getStringInput();
        System.out.print("소지하고 있는 금액이 얼마입니까? ");
        int money = getIntInput();

        System.out.println("안녕하세요, " + visitorName + "님! 동물원에 오신 것을 환영합니다.");
        System.out.println("당신의 소지금액은 " + money + "원 입니다.");

        // 입장료 확인 및 결제
        final int ENTRANCE_FEE = 5000;
        if (money < ENTRANCE_FEE) {
            System.out.println("죄송합니다. 입장료가 부족합니다. (입장료: " + ENTRANCE_FEE + "원)");
            return;
        }

        System.out.println("입장료 " + ENTRANCE_FEE + "원을 결제하시겠습니까? (1: 예 / 2: 아니오)");
        int payChoice = getIntInput();

        if (payChoice == 1) {
            money -= ENTRANCE_FEE;
            System.out.println("입장료 결제가 완료되었습니다. 남은 금액: " + money + "원");
            showVisitorMainMenu(visitorName, money);
        } else {
            System.out.println("입장을 취소하셨습니다.");
        }
    }

    // 관람객 메인 메뉴 - 소지금액을 매개변수로 전달
    private static void showVisitorMainMenu(String visitorName, int currentMoney) {
        int money = currentMoney; // 지역변수로 소지금액 관리

        while (true) {
            System.out.println("\n=== " + visitorName + "님의 메뉴 ===");
            System.out.println("1. 케이지목록보기  2. 이벤트 정보  3. 소지금액 보기  4. 체험하기  0. 메인메뉴로");
            System.out.print("선택해주세요: ");

            int choice = getIntInput();

            switch (choice) {
                case 1 -> showCageList();
                case 2 -> showEventInfo();
                case 3 -> showCurrentMoney(money);
                case 4 -> money = handleExperience(money); // 체험 후 변경된 금액 반환
                case 0 -> {
                    System.out.println("메인 메뉴로 돌아갑니다. 안전하게 귀가하세요!");
                    return;
                }
                default -> System.out.println("잘못된 입력입니다. 다시 선택해주세요.");
            }
        }
    }

    // 현재 소지금액 표시
    private static void showCurrentMoney(int money) {
        System.out.println("\n💰 현재 소지금액: " + money + "원");
        if (money < 1000) {
            System.out.println("⚠️  소지금액이 부족합니다. 일부 체험이 제한될 수 있습니다.");
        }
    }

    // 체험 메뉴 처리 - 소지금액을 매개변수로 받아 처리 후 반환
    private static int handleExperience(int currentMoney) {
        System.out.println("\n=== 체험 메뉴 ===");
        System.out.println("1. 동물 먹이주기 (2000원)  2. 포니 체험 (3000원)  3. 기념품 구매 (1500원)  0. 뒤로가기");
        System.out.print("선택해주세요: ");

        int choice = getIntInput();

        switch (choice) {
            case 1 -> currentMoney = processPayment(currentMoney, 2000, "동물 먹이주기 체험");
            case 2 -> currentMoney = processPayment(currentMoney, 3000, "포니 체험");
            case 3 -> currentMoney = processPayment(currentMoney, 1500, "기념품 구매");
            case 0 -> System.out.println("체험 메뉴를 종료합니다.");
            default -> System.out.println("잘못된 입력입니다.");
        }

        return currentMoney; // 변경된 금액 반환
    }

    // 결제 처리 메서드 - 소지금액과 결제금액을 매개변수로 받아 처리
    private static int processPayment(int currentMoney, int price, String itemName) {
        if (currentMoney < price) {
            System.out.println("❌ 소지금액이 부족합니다. (" + itemName + ": " + price + "원, 현재 소지금액: " + currentMoney + "원)");
            return currentMoney;
        }

        System.out.println(itemName + "을(를) " + price + "원에 이용하시겠습니까? (1: 예 / 2: 아니오)");
        int confirm = getIntInput();

        if (confirm == 1) {
            currentMoney -= price;
            System.out.println("✅ " + itemName + " 결제가 완료되었습니다!");
            System.out.println("💰 남은 금액: " + currentMoney + "원");

            // 체험별 특별 메시지
            switch (itemName) {
                case "동물 먹이주기 체험" -> System.out.println("🐰 동물들이 맛있게 먹고 있어요!");
                case "포니 체험" -> System.out.println("🐎 포니와 함께 즐거운 시간을 보내세요!");
                case "기념품 구매" -> System.out.println("🎁 멋진 기념품을 구매하셨습니다!");
            }
        } else {
            System.out.println("결제를 취소하셨습니다.");
        }

        return currentMoney;
    }

    // 동물 등록
    private static void registerAnimal() {
        System.out.println("\n동물 등록 기능입니다.");
        System.out.print("동물 이름을 입력하세요: ");
        String animalName = getStringInput();
        System.out.print("동물 종류를 입력하세요: ");
        String animalType = getStringInput();
        System.out.print("나이를 입력하세요: ");
        int age = getIntInput();
        System.out.print("케이지 번호를 입력하세요: ");
        int cageNumber = getIntInput();

        System.out.println("동물 '" + animalName + "'이(가) " + cageNumber + "번 케이지에 성공적으로 등록되었습니다.");
    }

    // 동물 조회
    private static void viewAnimals() {
        System.out.println("\n=== 등록된 동물 목록 ===");
        System.out.println("1. 호랑이 - 5세 (케이지 A-1)");
        System.out.println("2. 코끼리 - 12세 (케이지 B-2)");
        System.out.println("3. 기린 - 8세 (케이지 C-3)");
        System.out.println("4. 펭귄 - 3세 (케이지 D-4)");
        System.out.println("5. 원숭이 - 6세 (케이지 E-5)");
        System.out.println("(실제 데이터는 데이터베이스에서 조회됩니다)");
    }

    // 동물 수정
    private static void updateAnimal() {
        System.out.println("\n동물 정보 수정 기능입니다.");
        viewAnimals();
        System.out.print("수정할 동물 번호를 입력하세요: ");
        int animalId = getIntInput();
        System.out.print("새로운 나이를 입력하세요: ");
        int newAge = getIntInput();
        System.out.println("동물 정보가 수정되었습니다. (나이: " + newAge + "세)");
    }

    // 동물 삭제
    private static void deleteAnimal() {
        System.out.println("\n동물 삭제 기능입니다.");
        viewAnimals();
        System.out.print("삭제할 동물 번호를 입력하세요: ");
        int animalId = getIntInput();
        System.out.println("정말로 삭제하시겠습니까? (1: 예 / 2: 아니오)");
        int confirm = getIntInput();

        if (confirm == 1) {
            System.out.println("동물이 삭제되었습니다.");
        } else {
            System.out.println("삭제가 취소되었습니다.");
        }
    }

    // 케이지 관련 메서드들
    private static void registerCage() {
        System.out.println("\n케이지 등록 기능입니다.");
        System.out.print("케이지 이름을 입력하세요: ");
        String cageName = getStringInput();
        System.out.print("케이지 크기(㎡)를 입력하세요: ");
        int cageSize = getIntInput();
        System.out.print("최대 수용 동물 수를 입력하세요: ");
        int maxCapacity = getIntInput();

        System.out.println("케이지 '" + cageName + "'이(가) 성공적으로 등록되었습니다.");
        System.out.println("크기: " + cageSize + "㎡, 최대 수용량: " + maxCapacity + "마리");
    }

    private static void viewCages() {
        System.out.println("\n=== 케이지 목록 ===");
        System.out.println("A-1: 맹수관 (100㎡, 수용량: 2마리) - 호랑이 1마리");
        System.out.println("B-2: 초식동물관 (200㎡, 수용량: 3마리) - 코끼리 1마리");
        System.out.println("C-3: 사바나관 (300㎡, 수용량: 5마리) - 기린 1마리");
        System.out.println("D-4: 극지관 (150㎡, 수용량: 10마리) - 펭귄 1마리");
        System.out.println("E-5: 원숭이관 (80㎡, 수용량: 8마리) - 원숭이 1마리");
    }

    private static void updateCage() {
        System.out.println("\n케이지 수정 기능입니다.");
        viewCages();
        System.out.print("수정할 케이지 번호를 입력하세요: ");
        String cageId = getStringInput();
        System.out.println("케이지 " + cageId + " 정보가 수정되었습니다.");
    }

    private static void deleteCage() {
        System.out.println("\n케이지 삭제 기능입니다.");
        viewCages();
        System.out.print("삭제할 케이지 번호를 입력하세요: ");
        String cageId = getStringInput();
        System.out.println("케이지 " + cageId + "이(가) 삭제되었습니다.");
    }

    // 사육사 관련 메서드들
    private static void registerKeeper() {
        System.out.println("\n사육사 등록 기능입니다.");
        System.out.print("사육사 이름을 입력하세요: ");
        String keeperName = getStringInput();
        System.out.print("담당 케이지 번호를 입력하세요: ");
        String cageId = getStringInput();
        System.out.print("경력(년)을 입력하세요: ");
        int experience = getIntInput();

        System.out.println("사육사 '" + keeperName + "'님이 성공적으로 등록되었습니다.");
        System.out.println("담당 케이지: " + cageId + ", 경력: " + experience + "년");
    }

    private static void viewKeepers() {
        System.out.println("\n=== 사육사 목록 ===");
        System.out.println("1. 김동물 - A-1 케이지 담당 (경력 5년)");
        System.out.println("2. 이자연 - B-2 케이지 담당 (경력 3년)");
        System.out.println("3. 박동물원 - C-3 케이지 담당 (경력 8년)");
        System.out.println("4. 최펭귄 - D-4 케이지 담당 (경력 2년)");
        System.out.println("5. 정원숭이 - E-5 케이지 담당 (경력 4년)");
    }

    private static void updateKeeper() {
        System.out.println("\n사육사 정보 수정 기능입니다.");
        viewKeepers();
        System.out.print("수정할 사육사 번호를 입력하세요: ");
        int keeperId = getIntInput();
        System.out.println("사육사 정보가 수정되었습니다.");
    }

    private static void deleteKeeper() {
        System.out.println("\n사육사 삭제 기능입니다.");
        viewKeepers();
        System.out.print("삭제할 사육사 번호를 입력하세요: ");
        int keeperId = getIntInput();
        System.out.println("사육사가 삭제되었습니다.");
    }

    // 관람객 관련 메서드들 - 수정된 메뉴에 맞게 개선
    private static void showCageList() {
        System.out.println("\n=== 🏠 케이지 목록 및 동물 정보 ===");
        System.out.println("🐅 A-1 맹수관: 호랑이 '타이거' - 아시아 호랑이, 5세");
        System.out.println("   ▶ 먹이시간: 오전 11시, 오후 4시");
        System.out.println("🐘 B-2 초식동물관: 코끼리 '점보' - 아프리카 코끼리, 12세");
        System.out.println("   ▶ 먹이시간: 오전 10시, 오후 3시");
        System.out.println("🦒 C-3 사바나관: 기린 '목길이' - 아프리카 기린, 8세");
        System.out.println("   ▶ 먹이시간: 오전 9시, 오후 2시");
        System.out.println("🐧 D-4 극지관: 펭귄 '팽이' - 황제펭귄, 3세");
        System.out.println("   ▶ 먹이시간: 오전 11시 30분, 오후 4시 30분");
        System.out.println("🐒 E-5 원숭이관: 원숭이 '바나나' - 일본원숭이, 6세");
        System.out.println("   ▶ 먹이시간: 오전 10시 30분, 오후 3시 30분");

        System.out.println("\n💡 각 동물의 먹이시간에 맞춰 방문하시면 더욱 활발한 모습을 보실 수 있습니다!");
    }

    private static void showEventInfo() {
        System.out.println("\n=== 🎉 이벤트 정보 ===");
        System.out.println("🥕 동물 먹이주기 체험 (2000원)");
        System.out.println("   ▶ 시간: 매일 오후 2시");
        System.out.println("   ▶ 장소: 체험관");
        System.out.println("   ▶ 대상: 토끼, 염소, 양");

        System.out.println("🐎 포니 체험 (3000원)");
        System.out.println("   ▶ 시간: 매주 토, 일요일 오후 3시");
        System.out.println("   ▶ 장소: 승마체험장");
        System.out.println("   ▶ 대상: 어린이 (만 12세 이하)");

        System.out.println("🎁 기념품 구매 (1500원)");
        System.out.println("   ▶ 동물 인형, 엽서, 스티커 등");
        System.out.println("   ▶ 장소: 기념품샵");

        System.out.println("🌙 야간 동물원 투어 (무료)");
        System.out.println("   ▶ 시간: 매주 금요일 저녁 7시");
        System.out.println("   ▶ 야행성 동물들의 특별한 모습 관찰");
    }

    // 안전한 정수 입력 메서드
    private static int getIntInput() {
        while (true) {
            try {
                return Integer.parseInt(scanner.nextLine());
            } catch (NumberFormatException e) {
                System.out.print("올바른 숫자를 입력해주세요: ");
            }
        }
    }

    // 안전한 문자열 입력 메서드
    private static String getStringInput() {
        return scanner.nextLine().trim();
    }
}

```