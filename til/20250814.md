# Stream (스트림)

- stream 연산은 lazy 함.
- 중간연산들은 stream 을 반환하기 때문에 체인 방식으로 호출이 가능하다. 
- 당연하게도 마지막 연산은 값은 반환해야 함.
- 컬렉션이 공간적 집합체라면, 스트림은 시간적 집합체이다.

- 객체 스트림이 제공하는 최종 연산이 Optional 타입을 반환한다면 숫자는 OptionalInt, OptionalLong 과 같은 타입이 존재함.
- 숫자 스트림은 데이터 스트림의 기본 통계 요약 내용을 나타내는 summaryStatistics() 메서드를 제공함
